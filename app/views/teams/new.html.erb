<div class="main users-new">
  <div class="container">
    <div class="form-heading">チームを作成</div>
    <div class="form-heading-sub">新たにチームが作成され、あなたはチームのリーダーになります</div>
    <div class="form users-form">
      <div class="form-body">

        <% @team.errors.full_messages.each do |t| %>
          <div class="form-error">
            <%= t %>
          </div>
        <% end %>

        <%= form_with model: @team, local: true do |t| %>
          <%= t.label "チーム名" %>
          <%= t.text_field :name %>

          <%= t.submit "新規登録" %>
        <% end %>
      </div>
    </div>
  </div>
</div>
