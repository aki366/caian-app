<div class="main user-show">
  <div class="container">
    <div class="user">
      <img src= '<%= "/user_images/#{@user.user_image}" %>'> 

      <h2><%= @user.name %></h2>
      <p><%= @user.email %></p>
      <% if @user.id == @current_user.id && !@user.guest? %>
        <%= link_to("編集", "/users/#{@user.id}/edit") %>
      <% end %>
    </div>

    <ul class="user-tabs">
      <li><%= link_to("投稿", "/users/#{@user.id}") %></li>
      <li class="active"><%= link_to("いいね!", "/users/#{@user.id}/likes") %></li>
    </ul>

    <% @likes.each do |l| %>
      <div class="posts-index-item">
        <div class="post-left">
          <img src="<%= "/user_images/#{l.post.user.user_image}" %>">
        </div>
        <div class="post-right">
          <div class="post-user-name">
            <%= link_to(l.post.user.name, "/users/#{l.post.user.id}") %>
          </div>
          <%= link_to(l.post.content, "/posts/#{l.post.id}") %>
        </div>
      </div>
    <% end %>
  </div>
</div>
