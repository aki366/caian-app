<div class="main room-show">
  <div class="room-container">
    <h3>
      <% @not_current_room_user.each do |room| %>
        <div class="room-c-user-name">
          <%= link_to(room.user.name, "/users/#{room.user.id}") %>
        </div>
      <% end %>
    </h3>
    <br>
    <% if @room_user.present? %>
      <% @users.each do |users| %>
      <div class="room-show-line">
        <div class="line1"></div>
      </div>
      <div class="room-c-user">
        <img src='<%= "/user_images/#{users.user.user_image}" %>'>
      </div>
      <div class="room-c-user-name">
        <%= link_to(users.user.name, "/users/#{users.user.id}") %>
      </div>
      <div class="room-c-timestamp">
        <a data-stringify-type= r.created_at %>
        <%= users.created_at.strftime("%Y-%m-%d %H:%M") %>
      </div>
      <div class="overflow-wrap">
        <%= users.text %>
        <div class="room-c-menus">
          <% if users.user_id == @current_user.id %>
            <%= link_to "削除", room_message_path(@room, users), method: :delete %>
          <% end %>
        </div>
      </div>
      <% end %>
    <% else %>
      <h3 class="text-center">メッセージはまだありません</h3>
    <% end %>
    <%= form_with(model: [@room,  @message], local: true) do |f| %>
      <div class="c-form">
        <%= f.text_field :text, :placeholder => "メッセージを入力して下さい" , :size => 70 %>
        <%= f.hidden_field :room_id, :value => @room.id %>
        <%= f.submit "投稿する" %>
      </div>
    <% end %>
  </div>
</div>
